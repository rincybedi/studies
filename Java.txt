import java .util.*;

java 

Stack:

Stack<Integer> stack = new Stack();
stack.push(2)
stack.pop()

Queue: peek, poll, add(or offer)

ArrayList:

List<Integere>  list = new arrayList()
list.add(2)
list.get(0) //index
list.set(0, 3)//set by inex 
list.remove(index)
list.clear()
Collections.sort(list)
for(String s: cars){
	sout(s)
}

String s;
char[] ch = s.toChar(Array)

HashMap; <key, Value> hm
put, get , remove ,clear 
hm.values(), hm.keySet()
